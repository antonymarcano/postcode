{exec} = require 'child_process'

execute_command = (line)->
  exec line, (err, stdout, stderr) ->
    console.log stdout + stderr

task 'build', 'Build spec/js from spec/coffee', ->
  execute_command 'coffee --compile --output spec/js spec/coffee'

task 'test', 'Builds everything then executes Jasmine tests via phantomjs',  ->
  invoke 'build'
  execute_command 'phantomjs spec/phantom-jasmine/lib/run_jasmine_test.coffee spec/TestRunner.html'
